# android-ci-action
A GitHub Action that builds, signs, scans and releases Android apps using a reproducible, CI‑friendly workflow. Supports Gradle builds, secure keystore handling, APK signing, optional security scanning and automated release publishing.
